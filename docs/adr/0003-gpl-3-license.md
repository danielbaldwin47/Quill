# Quill is licensed GPL-3.0-or-later

Quill is positioned as an open alternative to a closed product, so forks must stay open. GTK is LGPL and the iA fonts are OFL, so the combination is clean. The rewrite ships under GPL-3.0-or-later from its first commit; the JavaScript app under `dev/legacy/` keeps its ISC license until it is deleted.

diff --git c/quill/src/editor.rs w/quill/src/editor.rs
index fe5fa76..cf0e1de 100644
--- c/quill/src/editor.rs
+++ w/quill/src/editor.rs
@@ -880,6 +880,7 @@ impl Editor {
 
     /// Captures dirty prose after the debounce, with the laid-out viewport first.
     pub(crate) fn submit_syntax(&self, document: &Document) {
+        let _probe = crate::harness::probe_stage("submit"); // DEBUG-327
         let at = self.furnished(document);
         let first = document.block_at(at.start).unwrap_or(0);
         let last = document.block_at(at.end).unwrap_or(first);
@@ -891,6 +892,7 @@ impl Editor {
 
     /// Drains a bounded idle batch and redraws only its accepted paragraphs.
     pub(crate) fn drain_syntax(&self, document: &Document) -> bool {
+        let _probe = crate::harness::probe_stage("drain"); // DEBUG-327
         let mut lines = self.imp().syntax.borrow_mut().drain(document);
         if !lines.is_empty() {
             // A fade's snapshots contain the old Category colours. Its whole
@@ -1638,6 +1640,7 @@ impl Editor {
     /// by the line and the byte index within it and both of those have to be
     /// the ones the writer can now see.
     pub fn retag(&self, document: &Document, edit: &Edit) {
+        let _probe = crate::harness::probe_stage("retag"); // DEBUG-327
         self.imp().syntax.borrow_mut().edited(document, edit);
         // An edit moves the caret as well as the text, so the tiers are worked
         // out again here rather than left to the caret's own feed: the lines
@@ -1798,6 +1801,7 @@ impl Editor {
     /// already right, and the fade is only holding the old colour on top of it
     /// for [`quill_engine::theme::FADE_MS`].
     fn begin_fade(&self, document: &Document, before: &[LineTiers], moved: &[Range<usize>]) {
+        let _probe = crate::harness::probe_stage("begin-fade"); // DEBUG-327
         if self.fade_length() == 0 || moved.is_empty() {
             return;
         }
@@ -1856,6 +1860,7 @@ impl Editor {
     /// since changed. A line number the edit has moved is a line drawn as it
     /// stands, which is never wrong, only wasted.
     fn settle_fade(&self, document: &Document) {
+        let _probe = crate::harness::probe_stage("settle-fade"); // DEBUG-327
         let Some(fade) = self.imp().fade.take() else {
             return;
         };
@@ -1882,6 +1887,7 @@ impl Editor {
             return;
         }
         self.add_tick_callback(move |editor, clock| {
+            let _probe = crate::harness::probe_stage("fade-tick"); // DEBUG-327
             if advance(editor, clock.frame_time()) {
                 return glib::ControlFlow::Continue;
             }
@@ -1898,6 +1904,7 @@ impl Editor {
     fn over_frames(&self, frames: u32, step: impl Fn(&Self) -> bool + 'static) {
         let left = Cell::new(frames);
         self.add_tick_callback(move |editor, _| {
+            let _probe = crate::harness::probe_stage("step-tick"); // DEBUG-327
             left.set(left.get().saturating_sub(1));
             if step(editor) || left.get() == 0 {
                 glib::ControlFlow::Break
@@ -2783,6 +2790,7 @@ impl Editor {
             return;
         }
         self.add_tick_callback(|editor, clock| {
+            let _probe = crate::harness::probe_stage("caret-tick"); // DEBUG-327
             editor.tell_caret(|caret| caret.tick(clock.frame_time()));
             editor.queue_draw();
             let caret = editor.imp().caret.get();
diff --git c/quill/src/harness.rs w/quill/src/harness.rs
index 26679f5..913cdad 100644
--- c/quill/src/harness.rs
+++ w/quill/src/harness.rs
@@ -49,6 +49,115 @@ use gtk::gdk;
 use gtk::glib;
 use gtk::prelude::*;
 
+// DEBUG-327: temporary observations; never used by measurement acceptance.
+thread_local! {
+    static PROBE: RefCell<Option<Probe>> = const { RefCell::new(None) };
+}
+
+struct Probe {
+    file: File,
+    lines: String,
+    watches: Vec<(gdk::FrameTimings, i64)>,
+}
+
+pub(crate) struct ProbeStage(&'static str, i64);
+
+pub(crate) fn probe_stage(name: &'static str) -> ProbeStage {
+    ProbeStage(name, glib::monotonic_time())
+}
+
+impl Drop for ProbeStage {
+    fn drop(&mut self) {
+        let end = glib::monotonic_time();
+        PROBE.with_borrow_mut(|held| {
+            if let Some(p) = held {
+                p.lines.push_str(&format!("{{\"kind\":\"stage\",\"name\":\"{}\",\"start_us\":{},\"end_us\":{end}}}\n", self.0, self.1));
+            }
+        });
+    }
+}
+
+fn probe_observe(t: &gdk::FrameTimings, consumer: &str) {
+    let before = glib::monotonic_time();
+    let present = t.presentation_time();
+    let complete = t.is_complete();
+    let after = glib::monotonic_time();
+    PROBE.with_borrow_mut(|held| {
+        if let Some(p) = held {
+            p.lines.push_str(&format!("{{\"kind\":\"accept\",\"consumer\":\"{consumer}\",\"frame\":{},\"before_us\":{before},\"after_us\":{after},\"complete\":{complete},\"present_us\":{present}}}\n", t.frame_counter()));
+            p.watches.push((t.clone(), after));
+        }
+    });
+}
+
+fn probe_phase(name: &str, clock: &gdk::FrameClock) {
+    let now = glib::monotonic_time();
+    let frame = clock.frame_counter();
+    let frame_time = clock.frame_time();
+    let predicted = clock
+        .timings(frame)
+        .map_or(0, |t| t.predicted_presentation_time());
+    PROBE.with_borrow_mut(|held| {
+        if let Some(p) = held {
+            p.lines.push_str(&format!(
+                "{{\"kind\":\"phase\",\"name\":\"{name}\",\"frame\":{frame},\"at_us\":{now},\"frame_time_us\":{frame_time},\"predicted_us\":{predicted}}}\n"
+            ));
+        }
+    });
+}
+
+// DEBUG-327: the main loop's busy spans, from one poll's return to the next
+// poll's entry, whenever one is longer than a millisecond.
+static PREV_POLL: std::sync::OnceLock<glib::ffi::GPollFunc> = std::sync::OnceLock::new();
+thread_local! {
+    static LAST_POLL_RETURN: std::cell::Cell<i64> = const { std::cell::Cell::new(0) };
+}
+
+unsafe extern "C" fn probe_poll(
+    ufds: *mut glib::ffi::GPollFD,
+    nfds: std::ffi::c_uint,
+    timeout: std::ffi::c_int,
+) -> std::ffi::c_int {
+    let enter = glib::monotonic_time();
+    let last = LAST_POLL_RETURN.with(|c| c.get());
+    if last != 0 && enter - last > 1000 {
+        PROBE.with_borrow_mut(|held| {
+            if let Some(p) = held {
+                p.lines.push_str(&format!("{{\"kind\":\"busy\",\"from_us\":{last},\"to_us\":{enter}}}\n"));
+            }
+        });
+    }
+    let prev = PREV_POLL.get().copied().flatten().expect("previous poll func");
+    let r = unsafe { prev(ufds, nfds, timeout) };
+    LAST_POLL_RETURN.with(|c| c.set(glib::monotonic_time()));
+    r
+}
+
+fn probe_poll_install() {
+    unsafe {
+        let ctx = glib::ffi::g_main_context_default();
+        let _ = PREV_POLL.set(glib::ffi::g_main_context_get_poll_func(ctx));
+        glib::ffi::g_main_context_set_poll_func(ctx, Some(probe_poll));
+    }
+}
+
+fn probe_drain() {
+    PROBE.with_borrow_mut(|held| {
+        if let Some(p) = held {
+            p.watches.retain(|(t, accepted)| {
+                let before = glib::monotonic_time();
+                let present = t.presentation_time();
+                let complete = t.is_complete();
+                let after = glib::monotonic_time();
+                p.lines.push_str(&format!("{{\"kind\":\"reread\",\"frame\":{},\"accepted_us\":{accepted},\"before_us\":{before},\"after_us\":{after},\"complete\":{complete},\"present_us\":{present}}}\n", t.frame_counter()));
+                after - accepted < 500_000
+            });
+            let _ = p.file.write_all(p.lines.as_bytes());
+            p.lines.clear();
+        }
+    });
+}
+
 /// The variable the harness stamps before `exec`, in realtime nanoseconds:
 /// what `date +%s%N` prints.
 const T0: &str = "QUILL_T0_NS";
@@ -106,6 +215,14 @@ thread_local! {
 /// kept for the rest of the process: a JSONL file truncated from the outside
 /// while its writer holds it open comes back NUL-padded.
 pub fn capture(out: &Path) {
+    probe_poll_install(); // DEBUG-327
+    PROBE.with_borrow_mut(|held| {
+        *held = Some(Probe {
+            file: File::create(out.with_extension("probe.jsonl")).unwrap(),
+            lines: String::new(),
+            watches: Vec::new(),
+        });
+    });
     match File::create(out) {
         Ok(file) => CAPTURE.with_borrow_mut(|held| {
             *held = Some(Capture {
@@ -151,10 +268,38 @@ pub fn watch(window: &impl IsA<gtk::Widget>) {
     widget.add_controller(pointer);
 
     widget.add_tick_callback(|widget, clock| {
+        // DEBUG-327: every phase of every frame, so a slow key's wait can be
+        // split into "waiting to be scheduled" and "drawing".
+        clock.connect_flush_events(|clock| probe_phase("flush", clock));
+        clock.connect_before_paint(|clock| probe_phase("before", clock));
+        clock.connect_update(|clock| probe_phase("update", clock));
+        clock.connect_layout(|clock| probe_phase("layout", clock));
+        clock.connect_paint(|clock| probe_phase("paint-start", clock));
+        clock.connect_resume_events(|clock| probe_phase("resume", clock));
         let painted = widget.downgrade();
         clock.connect_after_paint(move |clock| {
+            PROBE.with_borrow_mut(|held| {
+                if let Some(p) = held {
+                    p.lines.push_str(&format!("{{\"kind\":\"paint\",\"frame\":{},\"at_us\":{}}}\n", clock.frame_counter(), glib::monotonic_time()));
+                }
+            });
             // Every key stamped since the last paint was carried by this frame.
             mark(clock.frame_counter());
+            {
+                // DEBUG-327: when the main loop next has nothing left to do
+                // after this paint, which bounds when GTK flushed the commit.
+                let frame = clock.frame_counter();
+                glib::idle_add_local_once(move || {
+                    PROBE.with_borrow_mut(|held| {
+                        if let Some(p) = held {
+                            p.lines.push_str(&format!(
+                                "{{\"kind\":\"idle\",\"frame\":{frame},\"at_us\":{}}}\n",
+                                glib::monotonic_time()
+                            ));
+                        }
+                    });
+                });
+            }
             if let Some(widget) = painted.upgrade() {
                 drain(clock, &widget);
             }
@@ -247,7 +392,12 @@ fn presented(clock: &gdk::FrameClock) -> Option<i64> {
     (clock.history_start()..=clock.frame_counter())
         .filter_map(|counter| clock.timings(counter))
         .filter(gdk::FrameTimings::is_complete)
-        .map(|timings| timings.presentation_time())
+        .map(|timings| {
+            if timings.presentation_time() != 0 {
+                probe_observe(&timings, "cold");
+            }
+            timings.presentation_time()
+        })
         .find(|time| *time != 0)
 }
 
@@ -263,7 +413,12 @@ fn presented_from(clock: &gdk::FrameClock, frame: i64) -> Option<(i64, i64)> {
     (frame..=clock.frame_counter())
         .filter_map(|counter| clock.timings(counter))
         .filter(gdk::FrameTimings::is_complete)
-        .map(|timings| (timings.presentation_time(), timings.refresh_interval()))
+        .map(|timings| {
+            if timings.presentation_time() != 0 {
+                probe_observe(&timings, "key");
+            }
+            (timings.presentation_time(), timings.refresh_interval())
+        })
         .find(|(time, _)| *time != 0)
 }
 
@@ -291,8 +446,20 @@ fn mark(frame: i64) {
 /// and a frame is complete only once a later one has run.
 fn drain(clock: &gdk::FrameClock, widget: &gtk::Widget) {
     if with_capture(|capture| capture.drain(clock, glib::monotonic_time())) {
+        // DEBUG-327 round 3: the harness's own tail frame, the one thing that
+        // asks for a paint inside a deterministic pause.
+        PROBE.with_borrow_mut(|held| {
+            if let Some(p) = held {
+                p.lines.push_str(&format!(
+                    "{{\"kind\":\"tail\",\"frame\":{},\"at_us\":{}}}\n",
+                    clock.frame_counter(),
+                    glib::monotonic_time()
+                ));
+            }
+        });
         widget.queue_draw();
     }
+    probe_drain();
 }
 
 /// One key, from the moment it was seen to the moment its frame was presented.
diff --git c/quill/src/session.rs w/quill/src/session.rs
index 9a49060..634f866 100644
--- c/quill/src/session.rs
+++ w/quill/src/session.rs
@@ -1509,6 +1509,7 @@ pub fn watch_settings(app: &gtk::Application, session: &Rc<Session>) {
     let app = app.clone();
     let session = Rc::clone(session);
     glib::timeout_add_local(DRAIN_EVERY, move || {
+        let _probe = crate::harness::probe_stage("session-drain"); // DEBUG-327
         // The watch is held here and nowhere else, so it stops when the
         // application does. Every save waiting is one re-read: a file is read
         // whole, so reading it twice would apply the same file twice. The
diff --git c/quill/src/window.rs w/quill/src/window.rs
index bf7a1a8..f3f131c 100644
--- c/quill/src/window.rs
+++ w/quill/src/window.rs
@@ -849,6 +849,7 @@ impl Window {
             .replace(Some(glib::timeout_add_local_once(
                 quill_engine::worker::DEBOUNCE,
                 move || {
+                    let _probe = crate::harness::probe_stage("worker-debounce"); // DEBUG-327
                     let Some(window) = weak.upgrade() else {
                         return;
                     };
@@ -950,6 +951,7 @@ impl Window {
     /// is left for the close prompt to ask about, because a timer a second
     /// after a keystroke is not a question the writer asked.
     fn autosave(&self) {
+        let _probe = crate::harness::probe_stage("autosave"); // DEBUG-327
         self.flush();
     }
 
@@ -1096,6 +1098,7 @@ impl Window {
     /// to go, or a writer who asked to always be asked, gets the Save As
     /// dialog rather than nothing happening.
     pub(crate) fn save(&self) {
+        let _probe = crate::harness::probe_stage("save"); // DEBUG-327
         let state = self.imp().filed.borrow().state();
         match state {
             // A file that is gone comes back: `Filed::save` recreates it.
@@ -1767,6 +1770,7 @@ impl Window {
         }
         let ticking = self.downgrade();
         let id = glib::timeout_add_seconds_local(STATUS_TICK, move || {
+            let _probe = crate::harness::probe_stage("status-tick"); // DEBUG-327
             let Some(window) = ticking.upgrade() else {
                 return glib::ControlFlow::Break;
             };
diff --git c/tools/bench.mjs w/tools/bench.mjs
index 90f80ee..d210aa7 100644
--- c/tools/bench.mjs
+++ w/tools/bench.mjs
@@ -413,6 +413,8 @@ async function runSession(root, stage, { regime, keys, index }) {
     // The warm-up's events lead, in order; a run that lost focus inside the warm-up sent nothing.
     const sent = wrote.events.slice(warm.keys).map((e, i) => ({ ...e, i }));
     const seen = before === null ? [] : capture(out).slice(before);
+    // DEBUG-327: retain the exact inputs to the unchanged production join.
+    fs.writeFileSync(path.join(stage.tmp, `joined-${index}.json`), JSON.stringify({ sent, seen }, null, 2));
     const cold = /^cold start: ([\d.]+) ms$/m.exec(ours.said());
 
     return {
diff --git c/tools/harness.mjs w/tools/harness.mjs
index 6e1f057..7e69272 100644
--- c/tools/harness.mjs
+++ w/tools/harness.mjs
@@ -686,6 +686,8 @@ function closeStage({ state, owner, tmp, disarm }, restore) {
   try { restore(); } catch { /* ditto */ }
   if (owner.cursor) { try { movePointer(owner.cursor); } catch { /* ditto */ } }
   if (owner.window) { try { focusWindow(owner.window); } catch { /* ditto */ } }
+  // DEBUG-327: preserve every diagnostic attempt before the stage disappears.
+  fs.cpSync(tmp, path.join(process.env.QUILL_PROBE_ARCHIVE, path.basename(tmp)), { recursive: true });
   try { fs.rmSync(tmp, { recursive: true, force: true }); } catch { /* ditto */ }
   if (state.lock !== null) { try { fs.closeSync(state.lock); } catch { /* the exit releases it anyway */ } state.lock = null; }
   disarm();
